{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "outputDirectory": "dist",
  "buildCommand": "chmod +x ./node_modules/.bin/tsc && npm run build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist/server.js"
    }
  ]
}
